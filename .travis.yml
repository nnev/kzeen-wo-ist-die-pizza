language: ruby
cache: bundler
bundler_args: --deployment --local -j 4
script:
  - bundle exec gem install chromedriver-helper
  - bundle exec rake db:setup
  - bundle exec rspec
  - bundle exec rake assets:precompile
notifications:
  email:
    on_success: never # default: change
    on_failure: change # default: always
